#include <algorithm>
#include <atomic>
#include <cassert>
#include <execution>
#include <iostream>
#include <vector>

int main() {
#include "history_cc17_code.h"
  assert(cnt == 9);
  assert(sum == 45);
  std::cerr << ".";
}
