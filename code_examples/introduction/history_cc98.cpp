#include <algorithm>
#include <cassert>
#include <iostream>
#include <vector>

int main() {
#include "history_cc98_code.h"
  assert(result.cnt == 9);
  assert(result.sum == 45);
  std::cerr << ".";
}
