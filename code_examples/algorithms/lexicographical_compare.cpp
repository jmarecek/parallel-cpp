#include <algorithm>
#include <cassert>
#include <concepts>
#include <iostream>
#include <vector>

int main() {
#include "lexicographical_compare_code.h"
  assert(cmp1);
  assert(cmp2);
  assert(cmp3);
  assert(cmp4);

  std::cerr << ".";
}
