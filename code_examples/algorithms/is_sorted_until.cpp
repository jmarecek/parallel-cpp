#include <algorithm>
#include <cassert>
#include <concepts>
#include <iostream>
#include <vector>

int main() {
#include "is_sorted_until_code.h"
  assert(it == data.begin() + 3);
  assert(*it == 2);
  std::cerr << ".";
}
